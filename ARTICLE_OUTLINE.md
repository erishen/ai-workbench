# 文章提纲：让机器自己验证的 Agent

> 状态：提纲草稿（未发布、未提交）
> 去标识化要求：正文不点名任何具体模型/服务商（deepseek / agnes / scnet / kimi / minimax 等一律泛化为「兼容 OpenAI 接口的第三方模型 / 某国产大模型」）。源码引用块内出现真实名称可保留，但正文叙述不得把它们当作「作者专属使用」来点名。
> 定位：贴合「AI 工程化 / 落地交付」叙事，主打架构取舍与验证治理，不暗示 AI 算法深度。

---

## 一、候选标题（三选一）

1. **《吴恩达的 OpenWorker 让我审批，我做了个让机器自己验证的 Agent》** —— 借势叙事，对比强、有钩子。
2. **《审批门，还是自证门？两种智能体治理哲学的碰撞》** —— 偏思辨，适合技术社区。
3. **《不给 Agent 发"良民证"：用证据驱动验证给 AI 套上护栏》** —— 偏方法论，记忆点强。

（建议用 1，借 Andrew Ng 同赛道背书，降低读者认知门槛。）

## 二、目标读者与写作目的

- **读者**：做过/想做 AI Agent 的工程师、技术负责人、关注 AI 落地交付的招聘方。
- **目的**：① 建立「证据驱动验证」的方法论认知；② 把本项目定位为「会自证清白的 Agent / 验证治理增强层」，与通用 Agent（如 OpenWorker）区隔；③ 服务于个人技术品牌与求职（展示架构判断力，而非手写代码量）。

## 三、核心论点（Thesis，一句话）

> 通用 Agent 用「人审批门」治理（你点头它才动），但审批者未必懂技术、也未必保证产出正确；我用一个**独立的 Evaluator 角色**做「机器自证」——不采信执行者自述，靠命令取证给出结构化判定，必要时整步重试——把"活干完"升级成"活干对"。

## 四、正文大纲（8 节）

### 0. 引言（钩子，~300 字）
- 吴恩达 7 月底开源 OpenWorker：本地优先、模型无关、把活干完的桌面 AI 同事，治理靠「人审批门」。
- 我做了一个同赛道但治理模型不同的东西：让机器**自己验证自己**。
- 抛问题：当 Agent 越来越能干活，"它干得对不对"谁来保证？人批，还是机证？

### 1. 两种治理哲学（对比骨架，~600 字）
- **OpenWorker 式 = 人审批门**：工具按风险分级（读=低风险自动、写/发/执行=需人批），`ask_user` 把问题丢进 Inbox 挂起等人答，无人值守时审批堆积。
- **本项目式 = 机器自证门**：每个产出步骤后，独立 Evaluator 取证 → 结构化 verdict（PASS / PARTIAL / FAIL / BLOCKED）→ 不合格整步重跑。
- 一句话：前者把"对不对"的判断权留给人，后者把"对不对"的**取证权**交给机器、把"接不接受"的最终权仍留给人。

### 2. 为什么"人审批"不够（痛点，~500 字）
- 审批者需要懂技术才能批（"mvn test 过了没？"普通人答不了）。
- 批了 ≠ 干对（人只能看描述，看不到真实执行证据）。
- 无人值守时 Inbox 堆积，反而阻塞自动化。
- 引出：能不能让机器先自己拿出"证据"，人只做最终裁决？

### 3. PSE 四角色拆解（核心，~900 字，配架构图）
- **Planner**：产出带验收标准（AC）的结构化计划（JSON）。可选——留白时由模型自动拆解，预置时由人当导演。
- **Specialist**：真正干活的（写代码、建文件、跑命令）。
- **Evaluator**：**不采信执行者自述**，用命令/产物取证，输出固定 schema 的 verdict。这是差异化所在。
- **Reviewer**（可选第二道闸门）：独立再复核一遍。
- 关键设计：**受治理的自主**——每步"怎么干"模型自由，但"干什么 / 怎么算好 / 干不好怎么办"被系统拿住。

### 4. 自主 vs 编排：一条"规划权归属"谱系（你之前的洞察，~500 字）
- 留白 → Planner 自动拆解（自主型）；预置步骤 → 人当导演（编排型）。
- 设计器上的一个开关，就是在这条轴上滑动；甚至能逐步行级微调。
- 本质：同一个内核，因"规划权归属"不同，实例化成自主型或编排型。

### 5. 本机能力感知（工程细节，~400 字）
- 运行时先探测本机工具链（装了哪些 SDK/构建工具），注入 Planner。
- 价值：避免规划出"本机根本跑不了"的任务——这是自主型可靠性的底座。

### 6. 和 OpenWorker 怎么共存（格局，~400 字）
- 它偏通用办公同事（25+ SaaS 连接器 + MCP），我偏工程产出的验证治理。
- 本项目可定位为「通用 Agent 之上的验证治理增强层」。
- 它值得借鉴的：Tauri 轻量壳、异步审批 Inbox、有端到端测试。

### 7. 坦诚的成熟度（诚实声明，~300 字，符合个人定位）
- 当前是可用 demo 级、尚未端到端实测跑通、零自动化测试、模型通道未实测稳定。
- 下一步：补引擎层单测 + 本机端到端验证 + 冻结编排型聚焦自主。
- 不夸大、不包装成"成熟产品"——这反而增强可信度。

### 8. 结语 + 行动呼吁（~200 字）
- 智能体的下一战场不是"更自主"，而是"更可信"。
- 呼吁：把"验证治理"当成一等公民，而不是事后补丁。
- 抛出讨论点 / 开源仓库链接（去标识化后）。

## 五、配图与代码示意建议
- 图1：人审批门 vs 机器自证门 对比图。
- 图2：PSE 四角色单步执行时序（Specialist→Evaluator→verdict→重试）。
- 图3：自主—编排谱系图。
- 代码块：仅放「verdict 固定 schema」「Evaluator 取证提示词片段（泛化后）」「受治理自主的系统约束」等不暴露私人信息的片段。

## 六、发布与风险
- 渠道：技术博客 / 掘金 / 知乎 / Hacker News（英文版可投）。
- 红线：全程去标识化；不暴露私人用途（如个人投资分析）；开源仓库若列，不得点明其私人用途。
- 风险：若读者要求现场 demo，需本机端到端跑通（见第 7 节，先补测试再发）。

## 七、下一步动作（待你确认）
- [ ] 选定标题 → 扩写为 3000–4000 字正文
- [ ] 补 PSE 引擎单测 + 本机端到端验证（让文章"立得住"）
- [ ] 抓取本机运行截图/录屏做配图
- [ ] 去标识化终校后再发布
