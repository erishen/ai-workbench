# ============================================================
# 本地私密配置模板（本文件可提交到 git）
#
# 用法：复制本文件为 .env（即 `cp .env.example .env`），填入你的真实值。
# .env 已被 .gitignore 忽略，不会被提交，也不会打进分发给他人的安装包。
#
# 这几个变量只作为「开发者本地默认值」：
#   - 应用首次打开（userData 下还没有 settings.json）时，会用这里的值预填设置面板；
#   - 你在设置面板里保存后，值写入 userData/settings.json，覆盖这里的默认值；
#   - 打包后的安装包不含 .env，其他人首次打开会得到安全的空占位值，需自行填入自己的 Key。
# ============================================================

# OpenAI 兼容接口地址
BASE_URL=https://api.openai.com/v1

# 默认模型名
MODEL=gpt-4o-mini

# 你的 API Key（留空则首次打开需手动填写）
API_KEY=
